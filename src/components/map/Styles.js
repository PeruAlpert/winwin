import React from 'react'
import {
  StyleSheet,
} from 'react-native';

export const Styles = StyleSheet.create({

  map: {
    zIndex:-1,
    height: '100%',
    width: '100%',
    position: 'absolute',
    bottom: 0,
    right: 0,
    left: 0,
    top: 0
  }
});